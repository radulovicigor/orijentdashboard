# Orijent dashboard

Next.js app koji povlači podatke iz Meta Marketing API-ja i Shopify Admin API-ja.
Bez podešenih ključeva prikazuje demo podatke. `?demo=1` uvijek prikazuje demo.

## Environment variables (Vercel → Settings → Environment Variables)
Vidi `.env.example`. Nakon izmjene uradi Redeploy.
